<Window x:Class="LED_Controller.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="LED Controller" Height="680" Width="880" ResizeMode="CanResize">
    <Window.Resources>
        <!-- Etwas Abstand zwischen ListBox-Elementen -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top: Save/Load, ColorPicker, Senden -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,18" HorizontalAlignment="Left">
            <Button Content="Controller hinzufügen" Command="{Binding AddControllerCommand}" Width="150" Margin="0,0,14,0"/>
            <Button Content="Speichern" Command="{Binding SaveCommand}" Width="95" Margin="0,0,10,0"/>
            <Button Content="Laden" Command="{Binding LoadCommand}" Width="95" Margin="0,0,24,0"/>
            <xctk:ColorPicker SelectedColor="{Binding SelectedColor, Mode=TwoWay}" Width="200" Margin="0,0,10,0" />
            <Button Content="Farbe senden" Command="{Binding SendColorCommand}" Width="120" />
        </StackPanel>

        <!-- Hauptbereich: Controller-Liste -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Controllers}" SelectedItem="{Binding SelectedController, Mode=TwoWay}" MinHeight="340">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#666" BorderThickness="1" Padding="14" CornerRadius="8" Background="#FAFAFA">
                        <StackPanel>
                            <!-- Controller-Name, IP, Port + Löschbutton -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="Name:" Width="54" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="140"/>
                                <TextBlock Text="IP:" Margin="12,0,0,0" Width="20" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                <TextBlock Text="Port:" Margin="12,0,0,0" Width="36" VerticalAlignment="Center"/>
                                <TextBox Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Width="62"/>
                                <Button Content="✖" 
                                        Command="{Binding DataContext.RemoveControllerCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" 
                                        Margin="16,0,0,0" Width="38" Height="28"
                                        ToolTip="Controller löschen"/>
                            </StackPanel>

                            <!-- Strips (pro Controller) -->
                            <TextBlock Text="LED-Strips:" FontWeight="SemiBold" Margin="0,12,0,2"/>
                            <ItemsControl ItemsSource="{Binding Strips}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                            <TextBlock Text="Bezeichnung:" Width="86" VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                                            <TextBlock Text="LEDs:" Margin="12,0,0,0" Width="36" VerticalAlignment="Center"/>
                                            <TextBox Text="{Binding LedCount, UpdateSourceTrigger=PropertyChanged}" Width="56"/>
                                            <TextBlock Text="Typ:" Margin="12,0,0,0" Width="28" VerticalAlignment="Center"/>
                                            <ComboBox Width="70" SelectedItem="{Binding Type, UpdateSourceTrigger=PropertyChanged}">
                                                <ComboBoxItem Content="RGB"/>
                                                <ComboBoxItem Content="RGBW"/>
                                            </ComboBox>
                                            <Button Content="✖" 
                                                    Command="{Binding DataContext.RemoveStripCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}" Width="34" Height="24"
                                                    ToolTip="Strip löschen" Margin="12,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <!-- Strip hinzufügen -->
                            <Button Content="Strip hinzufügen" Width="148" Margin="0,10,0,0"
                                    Command="{Binding DataContext.AddStripCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Statusanzeige unten -->
        <Border Grid.Row="2" Margin="0,18,0,0" Background="#EEE" Padding="8,4" CornerRadius="6">
            <TextBlock Text="{Binding StatusMessage}" Foreground="DarkGreen" FontWeight="SemiBold" />
        </Border>
    </Grid>
</Window>
